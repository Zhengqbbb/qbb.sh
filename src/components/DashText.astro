<svg viewBox="0 0 800 200" {...Astro.props}>
    <text
        y="50%"
        dominant-baseline="middle"
        lengthAdjust="spacingAndGlyphs"
        {...Astro.props.center
            ? { 'text-anchor': 'middle', 'x': '50%' }
            : { x: '0%' }
        }
    >
        <slot />
    </text>
</svg>
